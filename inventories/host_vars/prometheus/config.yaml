# @format
---
## debian-13-setup variables
# Datetime
debian_datetime_ntp_server: 172.28.10.1

# User
debian_user_name: melanie
debian_user_ssh_public_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILmxlXueWqzE8G+MicNIbZOlznlKBNHd268MYlyNl98H melanie@metrics.tinynamoo.com"

## NFSv4-client-setup variables
# Kerberos
nfs_krb5_host_keytab_file: "/mnt/veracrypt2/Kerberos Keytabs/[Melanie] Prometheus/host.keytab"
nfs_krb5_user_keytab_file: "/mnt/veracrypt2/Kerberos Keytabs/[Melanie] Prometheus/user.keytab"

# NFS
nfs_user_name: "{{ nfs_clients['prometheus'].name }}"
nfs_user_id: "{{ nfs_clients['prometheus'].uid }}"
nfs_mounts:
  melanie:
    mount_share: /melanie
    mount_point: /srv/melanie

## NGINX variables
nginx_conf_files:
  - filename: prometheus.conf
    path: templates/nginx.prometheus.conf

## Prometheus variables
prometheus_monitoring_group_name: "{{ nfs_groups['monitoring'].name }}"
prometheus_monitoring_group_gid: "{{ nfs_groups['monitoring'].gid }}"

## Proxmox instance variables
proxmox_snapshot_vmid: 10191
proxmox_snapshot_retention: 3
proxmox_snapshot_include_ram: false

## Docker variables
docker_project_name: prometheus
docker_project_src: /opt/prometheus
